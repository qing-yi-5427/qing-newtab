{
  "name": "qing-newtab",
  "version": "1.12.0",
  "private": true,
  "type": "module",
  "description": "简洁、轻量的 Edge 新标签页扩展。",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm run test"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "jsdom": "^29.1.1"
  }
}
