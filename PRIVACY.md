# qing-newtab 隐私说明

更新日期：2026 年 8 月 24 日

qing-newtab 是一款替换浏览器新标签页的扩展。扩展开发者不会建立账号系统，也不会收集、出售或用于广告分析用户数据。

## 本地保存的数据

以下内容仅保存在浏览器提供的本地扩展存储中：

- 快捷方式、布局、主题、壁纸与搜索设置；
- 用户主动选择的自定义图标和壁纸；
- 自定义大模型接口地址、模型名称和 API 密钥；
- 网站图标与 Bing 每日壁纸的临时缓存。

API 密钥不会包含在配置备份中。扩展不会把本地设置同步到开发者服务器。

用户可以主动开启浏览器账号同步。开启后，普通设置、快捷方式名称与网址会写入浏览器提供的扩展同步存储；API 密钥、自定义壁纸和以本地图片保存的图标不会同步。快捷方式中的内网网址属于普通快捷方式数据，也会同步。关闭此功能后，扩展不再继续写入同步存储。

## 书签访问

扩展使用书签权限在新标签页读取并展示浏览器书签。只有在用户主动导入 iTab 备份时，扩展才会在书签栏创建或更新 `local` 文件夹。书签不会发送给开发者。

## 网络请求

扩展会在实现用户选择的功能时连接以下第三方服务：

- 获取 Bing 每日壁纸；
- 从目标网站、浏览器图标接口、icon.horse、favicon.im、Google 网站图标服务、Apple 静态资源服务或 iTab 备份中自带的图标地址获取网站图标；
- 把搜索词发送到用户选择的搜索引擎；
- 把用户主动提交的对话内容发送到所选大模型网站，或发送到用户自行配置的兼容接口；
- 使用自定义大模型接口时，把 API 密钥作为授权信息仅发送到用户填写的接口地址。

上述第三方服务会依照各自的隐私政策处理请求。qing-newtab 不会把这些请求转发到开发者控制的服务器。

## 快速对话

使用大模型网页快速对话时，扩展会把待发送内容短暂写入浏览器本地扩展存储，在目标网页完成填入与发送后立即删除。使用自定义接口产生的当前对话记录只保留在新标签页的运行内存中，关闭或刷新页面后清除。

## 数据共享与保留

扩展开发者不会接收用户数据，因此不会保存或出售用户数据。用户可以在右键菜单的设置中清理缓存，或通过浏览器卸载扩展来删除扩展本地数据。

## 联系方式

如对本隐私说明有疑问，请通过项目仓库联系开发者：

https://github.com/qing-yi-5427/qing-newtab
